import { NativeBox } from "./native";
import { CogneeValue } from "./value";
import { TaskContext } from "./task-context";
export type TaskFn = (input: CogneeValue, ctx?: TaskContext) => CogneeValue | Promise<CogneeValue>;
export type IterTaskFn = (input: CogneeValue, ctx?: TaskContext) => CogneeValue[] | Promise<CogneeValue[]>;
export type BatchTaskFn = (inputs: CogneeValue[], ctx?: TaskContext) => CogneeValue | Promise<CogneeValue>;
export interface TaskOptions {
    name?: string;
    batchSize?: number;
    weight?: number;
    summaryTemplate?: string;
}
/** Opaque handle wrapping a cognee-core TaskInfo (task + metadata). */
export declare class TaskInfo {
    /** @internal */
    readonly _box: NativeBox;
    /** @internal */
    constructor(box_: NativeBox);
}
/** Create a single-value async task from a JS function. */
export declare function createTask(fn: TaskFn, options?: TaskOptions): TaskInfo;
/** Create an iterator task from a JS function that returns an array. */
export declare function createIterTask(fn: IterTaskFn, options?: TaskOptions): TaskInfo;
/** Create a batch task from a JS function that receives an array. */
export declare function createBatchTask(fn: BatchTaskFn, options?: TaskOptions): TaskInfo;
//# sourceMappingURL=task.d.ts.map