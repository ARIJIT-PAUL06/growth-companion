"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RunHandle = void 0;
const native_1 = require("./native");
/** Handle to a pipeline run executing in the background. */
class RunHandle {
    /** @internal */
    constructor(box_) {
        this._box = box_;
    }
    /** Whether the background run has completed (success or failure). */
    get isFinished() {
        return native_1.native.runHandleIsFinished(this._box);
    }
    /** Request cancellation of the background run. */
    abort() {
        native_1.native.runHandleAbort(this._box);
    }
    /** Wait for completion. Returns the output values. Consumes the handle. */
    async wait() {
        return native_1.native.runHandleWait(this._box);
    }
}
exports.RunHandle = RunHandle;
//# sourceMappingURL=run-handle.js.map