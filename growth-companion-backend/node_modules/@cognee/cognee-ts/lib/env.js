"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_fs_1 = require("node:fs");
const node_path_1 = require("node:path");
const dotenv_1 = __importDefault(require("dotenv"));
const disabled = process.env.COGNEE_DISABLE_DOTENV;
if (disabled === "1" || disabled === "true") {
    // Opt out explicitly for hosts that manage env loading themselves.
}
else {
    const cwd = process.cwd();
    const candidates = [(0, node_path_1.resolve)(cwd, ".env"), (0, node_path_1.resolve)(cwd, "..", ".env")];
    for (const file of candidates) {
        if (!(0, node_fs_1.existsSync)(file)) {
            continue;
        }
        dotenv_1.default.config({ path: file });
        break;
    }
}
//# sourceMappingURL=env.js.map